# DEMO_1 — 项目纪律

本文是 DEMO_1 的唯一执行纪律真源。冲突时依次遵循：用户显式指令 > 本文 > `docs/GAME-DESIGN.md` > 默认行为。默认使用中文。

## 1. 真源与优先级

- 设计规则以 `docs/GAME-DESIGN.md` 为权威来源，代码实现必须与文档一致
- 无法确认的设计含义不凭经验补齐，标记"待确认"并向用户说明
- 重要背景、决策、待确认项和验收结论必须留在仓库，不只留在对话

## 2. 最简设计与反过度纪律

- Plan、设计和实施只以用户明确目标和已确认真源为边界；每项新增工作必须对应明确验收或既有需求，否则不纳入
- 优先复用现有技术、模式和接口，以最小复杂度满足当前任务；不得因"可能以后需要"新增模块、抽象层、兼容层或开关
- **内置能力直接调用**：平台或框架已提供的能力，直接调用；禁止用包装器、临时文件或间接方式证明它是否可用
- **新增复杂度必须举证**：只有新增机制承担不可由现有能力完成的独立职责，且收益和测试证据均明确时才允许引入；无法举证时一律采用更简单方案
- **删除失效复杂度**：方案调整后不再承担职责的代码、配置、测试和文档必须同步删除，不保留增加认知负担的死代码
- 发现范围外的顺手优化时，先向用户说明价值和影响，未经确认不写入当前范围或实现
- 只做当前任务所需的最小改动；不顺手重命名、格式化、删除或重构无关内容

### 设计方案最小化

- **讨论顺序**：先确认目标与范围、整体框架结论，再讨论功能细节和实现计划；仅在整体框架确认后讨论字段、交互、状态、接口细节
- **最小闭环交付**：设计只覆盖达成目标所必需的最短调用链、最少状态和最小变更面；评审时主动检查是否存在可删除的中间层、重复校验和过度防御
- **轻量路径**：单点且范围、影响、验收明确的事项可走轻量路径，无须新增框架文档或拆分步骤，但仍须先简要确认边界和验收

### Pre-Commit 三项自检

每次输出代码前必须过一遍：

1. **删掉 20% 代码功能还完整吗？** → 完整就删掉，那是过度设计
2. **这个模式解决的是当前问题还是想象中的问题？** → 想象的就重写
3. **新同事 1 分钟能看懂吗？** → 看不懂就简化

### Emergency Brake

发现自己在为不到 100 行的功能写 Factory / Strategy / Builder / Facade / 抽象基类时——立即停下来，删掉重写。

## 3. 技术栈与分层边界

- Vite + TypeScript（strict 模式）+ 原生 Canvas + Vitest
- 逻辑层（`src/core/`）：纯 TS，零 DOM/Canvas 依赖，必须可单测
- 渲染层（`src/render/`）：Canvas 绘制与交互，可引用逻辑层，不反向依赖
- 配置层（`src/config/`）：所有游戏数据为 TS 配置对象，数值不硬编码到逻辑中
- UI 层（`src/ui/`）：HTML DOM 元素（顶栏、面板），不依赖前端框架
- 新增一级目录前同步更新本文和 README

## 4. 数据驱动原则

- 兵种属性、地形效果、伤害×护甲矩阵、技能定义、地图布局、AI 参数、增援事件——全部为配置文件
- 新增内容（地形/兵种/技能/关卡）= 新增配置条目，不改代码
- 特性修正管线：基础规则层的数值和行为均可被特性改写，实现为可组合的修饰器

## 5. 编码规范

- TypeScript strict 模式，禁止 `any`（除非有明确注释说明理由）
- 函数和类型命名用英文，注释用中文（仅在 WHY 非显然时加注释）
- 六边形坐标统一使用轴坐标 `(q, r)`，尖顶朝向
- 渲染相关常量（颜色、尺寸）集中在渲染模块，不污染逻辑层
- 测试文件与源文件同名：`hex.ts` → `hex.test.ts`
- 功能开发与缺陷修复默认 TDD：先写失败用例，再最小实现，最后验证通过

## 6. 验证与证据纪律

- **证据优先，禁止臆测**：每项判断须对应当前可核查的文件内容、命令输出或测试结果；不以缺失推断否定——未看到某文件只能证明当前检查范围内未发现，不能断言其不存在
- **完成声明须有新鲜验证**：修改、修复、测试通过等结论必须基于本轮实际执行并确认结果的验证；不能引用猜测、旧输出或自述
- **证据不足即先核查**：可通过现有工具直接验证的事项，必须先执行目标检查；无法核查时明确说明限制
- 所有"完成""修复""通过"必须有实际命令输出支撑，不凭记忆断言
- 逻辑层改动至少运行 `npm test`；触及渲染、交互或构建时补充 `npm run build`
- 验证输出不得泄露敏感值
- 发现非自己创建的运行时、构建产物、未知文件或配置异常时，暂停修改，说明位置和来源，未经用户指示不删除或纳入范围

### 意外变更处理

发现自己未创建的文件、配置、脚本、锁文件、构建产物、冲突标记或敏感信息时：

1. **暂停**：不删除、不回退、不提交、不纳入范围
2. **说明**：报告位置、来源推测和影响范围
3. **等待**：由用户决定处理方式（保留/删除/归档）

## 7. 代码变更纪律

- 每次完成代码编写或修改后，必须对本轮变更进行 Review：检查 diff、影响范围、是否符合分层边界和规范、是否需要补充测试、是否已执行必要校验命令
- Review 发现的问题必须先修复，再给出完成说明
- 变更前先 `git status --short --branch` 和 `git fetch --all --prune`，确认工作区干净且与远端同步
- 提交信息用中文，格式：`类型: 简述`（如 `feat: 六边形移动范围BFS`）
- 类型：`feat` / `fix` / `docs` / `refactor` / `test` / `chore`
- 单个 commit 只承载一个完整主题；只暂存目标文件，禁止 `git add .`
- 未经用户确认，不新增远端、不改写历史、不执行 `reset --hard` 或强推
- 远端：`git@github.com:wlzhou-1987/SLG-DEMO-1.git`（SSH）
- 分支命名：`main` 为唯一长期分支；功能分支 `feat/<简短描述>`（如 `feat/m2-hex-rendering`）；修复分支 `fix/<简短描述>`；文档分支 `docs/<简短描述>`；描述用英文短横线分隔
- 每次代码、配置或文档变动后，检查设计文档和本文是否需要同步

## 8. 目录结构

```
src/
  core/          # 逻辑层：hex数学、地图、单位、范围计算、战斗结算、AI
  config/        # 数据配置：地形、兵种、技能、法术、地图布局
  render/        # 渲染层：Camera、HexRenderer、InputHandler
  ui/            # HTML UI：topbar、sidepanel
  game.ts        # 游戏主循环与状态协调
  main.ts        # 入口
tests/
  core/          # 逻辑层单测（与 src/core/ 镜像）
docs/
  GAME-DESIGN.md # 设计文档（权威）
  prototypes/    # UI 原型（参考用）
```

## 9. 里程碑

| 里程碑 | 内容 | 验收标准 |
| --- | --- | --- |
| M1 | 设计定稿 | ✅ §2~§9 全部确认，GAME-DESIGN.md 定稿 |
| M2 | 工程与棋盘：渲染+选择+范围 | ✅ 20×30 棋盘渲染、50 单位显示、点选显示移动/攻击范围、拖拽平移/滚轮缩放、右侧面板显示单位信息、`vitest run` 全通过（60）、`npm run build` 无错误（2026-09-04） |
| M3 | 行动与战斗：移动/技能/朝向/预报 | ✅ 单位可移动、攻击结算（含反击/追击）、战斗预报显示、朝向确认、已行动置灰、敌方回合可自动行动（98 测试全绿，2026-09-04；敌方为占位 AI，完整版 M5） |
| M4 | 法术与状态：咏唱/延时/持续/护盾 | 法术释放（即时/咏唱）、延时/持续效果、护盾吸收、状态图标显示 |
| M5 | 回合与AI：三类激活/增援/胜负 | 三类 AI 激活模式、增援事件触发、胜负判定（全灭/领主阵亡）、多组敌方布防 |
| M6 | 打磨：反馈/面板/平衡 | 伤害数字飘字、动画过渡、信息面板完善、数值平衡可调 |
